import { Table } from 'antd'
import styled from 'styled-components'

/**
 * 预设配置列表表格
 */
export const PresetsTable = styled(Table)`` as typeof Table

/**
 * 预设配置名称文本
 */
export const PresetName = styled.span`
  font-weight: 400;
`
