/**
 * Schema Element Editor Host SDK - Core Entry
 * 纯 JS 核心逻辑导出入口
 */

// 导出核心函数
export { createSchemaElementEditorBridge } from './bridge'

// 注意：类型从主入口 (@schema-element-editor/host-sdk) 导出
// 如需类型，请从主入口导入：import type { ... } from '@schema-element-editor/host-sdk'
